{
  "name": "tsconline-monorepo",
  "packageManager": "yarn@4.0.2",
  "private": true,
  "workspaces": [
    "app",
    "shared",
    "server"
  ],
  "scripts": {
    "setup": "yarn && yarn install && yarn build",
    "install": "echo \"Installing packages of all workspaces...\" && yarn workspaces foreach -A --exclude @tsconline/shared --exclude tsconline/mono-repo -p -v run install",
    "build": "echo \"Building all workspaces...\" && yarn workspace @tsconline/shared run build && echo \"@tsconline/shared successfully built... \" && yarn workspaces foreach -A --exclude @tsconline/shared --exclude tsconline/mono-repo -p run build",
    "start": "yarn workspace @tsconline/server start"
  },
  "dependencies": {
    "fastify": "^4.13.0",
    "fs": "^0.0.1-security",
    "nvm": "^0.0.4",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@yarnpkg/sdks": "^3.0.0-rc.43"
  }
}
