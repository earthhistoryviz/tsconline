import { observer } from "mobx-react-lite";
import React from "react";
export const MapPoint = observer(function MapPoint() {
    return (
        <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center'}}>This is Map Point</div>
    )
}

)