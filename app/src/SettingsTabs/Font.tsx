import { observer } from "mobx-react-lite";
import React from "react";
export const Font = observer(function Font() {
    return (
        <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center'}}>This is Font</div>
    )
}

)