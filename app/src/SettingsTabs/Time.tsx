import { observer } from "mobx-react-lite";
import React from "react";
export const Time = observer(function Time() {
    return (
        <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center'}}>This is Time</div>
    )
}

)