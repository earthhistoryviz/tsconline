// This is suppose to be a component so it nests inside the
import React from "react";

const HelpDualColumnComparison = () => {
  return (
    
    <h1> Dual Column Comparison </h1>
  );
};

export default HelpDualColumnComparison;