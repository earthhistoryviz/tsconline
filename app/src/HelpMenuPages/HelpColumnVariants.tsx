// This is suppose to be a component so it nests inside the
import React from "react";

const HelpColumnVariants = () => {
  return (
    
    <h1> Column Variants </h1>
  );
};

export default HelpColumnVariants;